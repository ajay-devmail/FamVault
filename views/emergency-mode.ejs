<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FamVault - Emergency Mode</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        body { background-color: #0f172a; color: #e2e8f0; font-family: 'Inter', sans-serif; }
        .bg-card { background-color: #1e293b; }
    </style>
</head>
<body>

    <nav class="border-b border-red-800/50 bg-red-950/30 px-8 py-4 flex justify-between items-center">
        <a href="/overview" class="flex items-center gap-2 text-slate-300 hover:text-white transition">
            <i data-lucide="arrow-left" class="w-5 h-5"></i>
            Exit Emergency Mode
        </a>
        <div class="flex items-center gap-2 text-red-500 font-bold animate-pulse">
            <i data-lucide="alert-triangle" class="w-5 h-5"></i>
            EMERGENCY ACTIVE
        </div>
        <div></div>
    </nav>

    <main class="max-w-3xl mx-auto px-6 py-8 space-y-8">

        <div class="bg-card p-8 rounded-2xl shadow-lg border-l-4 border-red-500 flex flex-col md:flex-row gap-8">
            <div class="flex-1">
                <h1 class="text-4xl font-bold text-white mb-2"><%= user.name %></h1>
                <p class="text-slate-400 mb-1">
                    <span class="font-bold text-slate-300">DOB:</span> <%= user.dob || 'N/A' %> 
                    <% if(typeof age !== 'undefined') { %> (<%= age %> yrs) <% } %>
                </p>
                <p class="text-slate-400 mb-1">
                    <span class="font-bold text-slate-300">Gender:</span> <%= user.gender || 'N/A' %>
                </p>
                <div class="mt-4 flex items-center gap-2 text-green-400">
                    <i data-lucide="phone" class="w-4 h-4"></i>
                    <span class="font-mono font-bold"><%= user.phone || 'No personal phone set' %></span>
                </div>
            </div>
            
            <div class="flex-1 grid grid-cols-2 gap-6">
                <div>
                    <h3 class="text-xs text-slate-500 font-bold uppercase tracking-wider mb-2">Known Allergies</h3>
                    <p class="text-white font-medium"><%= user.allergies || 'None' %></p>
                </div>
                <div>
                    <h3 class="text-xs text-slate-500 font-bold uppercase tracking-wider mb-2">Medical Conditions</h3>
                    <p class="text-white font-medium"><%= user.conditions || 'None' %></p>
                </div>
                <div class="col-span-2">
                    <span class="inline-flex items-center gap-1 bg-red-900/50 text-red-200 text-sm font-semibold px-3 py-1 rounded-full">
                        <i data-lucide="droplet" class="w-4 h-4"></i> Blood Type: <%= user.bloodGroup || 'Unknown' %>
                    </span>
                </div>
            </div>
        </div>

        <div>
            <div class="flex items-center justify-between mb-4">
                <h2 class="text-xl font-bold flex items-center gap-3">
                    <i data-lucide="users" class="w-6 h-6 text-red-500"></i>
                    Trusted Contacts
                </h2>
                <button id="addContactBtn" class="flex items-center gap-2 text-sm text-blue-400 hover:text-blue-300 transition">
                    <i data-lucide="plus-circle" class="w-5 h-5"></i>
                    Add New
                </button>
            </div>

            <div id="addContactForm" class="hidden bg-card p-6 rounded-2xl mb-6 border border-slate-700">
                <h3 class="text-lg font-semibold text-white mb-4">Add New Contact</h3>
                <form action="/add-contact?from=emergency" method="POST" class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <input type="text" name="name" placeholder="Contact Name" class="bg-slate-800 border border-slate-700 text-white rounded-lg px-4 py-3 focus:outline-none focus:border-blue-500 w-full" required>
                    <input type="text" name="relationship" placeholder="Relationship (e.g. Spouse)" class="bg-slate-800 border border-slate-700 text-white rounded-lg px-4 py-3 focus:outline-none focus:border-blue-500 w-full" required>
                    <input type="tel" name="phone" placeholder="Phone Number" class="md:col-span-2 bg-slate-800 border border-slate-700 text-white rounded-lg px-4 py-3 focus:outline-none focus:border-blue-500 w-full" required>
                    
                    <div class="md:col-span-2 flex justify-end gap-3 mt-2">
                        <button type="button" id="cancelBtn" class="px-4 py-2 text-slate-400 hover:text-white transition">Cancel</button>
                        <button type="submit" class="px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition font-semibold">Save to Database</button>
                    </div>
                </form>
            </div>
            
            <div class="space-y-4">
                <% if(contacts.length === 0) { %>
                    <div class="text-center py-8 bg-card border border-dashed border-slate-700 rounded-xl text-slate-500">
                        No trusted contacts added yet. Click "Add New" to begin.
                    </div>
                <% } else { %>
                    <% contacts.forEach(contact => { %>
                    <div class="block bg-card p-5 rounded-2xl border border-slate-700/50 hover:border-slate-600 transition">
                        <div class="flex justify-between items-center">
                            <div class="flex items-center gap-4">
                                <div class="bg-green-500/20 p-3 rounded-full text-green-500">
                                    <i data-lucide="phone" class="w-6 h-6"></i>
                                </div>
                                <div>
                                    <h3 class="text-lg font-bold text-white"><%= contact.name %></h3>
                                    <p class="text-slate-400"><%= contact.relationship %></p>
                                </div>
                            </div>
                            <div class="text-right">
                                <div class="text-xl font-semibold text-green-500 select-all"><%= contact.phone %></div>
                                <a href="tel:<%= contact.phone %>" class="text-xs text-slate-400 hover:text-white underline">Tap to Call</a>
                            </div>
                        </div>
                    </div>
                    <% }) %>
                <% } %>
            </div>
        </div>

    </main>

    <script>
        lucide.createIcons();

        const addBtn = document.getElementById('addContactBtn');
        const cancelBtn = document.getElementById('cancelBtn');
        const formContainer = document.getElementById('addContactForm');

        addBtn.addEventListener('click', () => {
            formContainer.classList.remove('hidden');
            addBtn.classList.add('hidden');
        });

        cancelBtn.addEventListener('click', () => {
            formContainer.classList.add('hidden');
            addBtn.classList.remove('hidden');
        });
    </script>
</body>
</html>
