<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FamVault - Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-gray-50 text-slate-800">

    <div class="flex h-screen overflow-hidden">
        
        <aside class="w-64 bg-white border-r border-gray-200 hidden md:flex flex-col">
            <div class="p-6 flex items-center gap-3">
                <div class="bg-blue-600 p-1.5 rounded-lg">
                    <i data-lucide="shield" class="w-6 h-6 text-white fill-current"></i>
                </div>
                <span class="text-xl font-bold text-slate-900">FamVault</span>
            </div>

            <nav class="flex-1 px-4 space-y-1 mt-4">
                <a href="/overview" class="flex items-center gap-3 px-4 py-3 bg-blue-600 text-white rounded-lg transition-colors shadow-sm shadow-blue-200">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i>
                    <span class="font-medium">Overview</span>
                </a>
                <a href="/documents" class="flex items-center gap-3 px-4 py-3 text-slate-600 hover:bg-slate-50 hover:text-blue-600 rounded-lg transition-colors group">
                    <i data-lucide="file-text" class="w-5 h-5 group-hover:text-blue-600"></i>
                    <span class="font-medium">Documents</span>
                </a>
                <a href="/medical-records" class="flex items-center gap-3 px-4 py-3 text-slate-600 hover:bg-slate-50 hover:text-blue-600 rounded-lg transition-colors group">
                    <i data-lucide="heart-pulse" class="w-5 h-5 group-hover:text-blue-600"></i>
                    <span class="font-medium">Medical Records</span>
                </a>
                <a href="/emergency-contacts" class="flex items-center gap-3 px-4 py-3 text-slate-600 hover:bg-slate-50 hover:text-blue-600 rounded-lg transition-colors group">
                    <i data-lucide="phone" class="w-5 h-5 group-hover:text-blue-600"></i>
                    <span class="font-medium">Emergency Contacts</span>
                </a>
                <a href="/reminders" class="flex items-center gap-3 px-4 py-3 text-slate-600 hover:bg-slate-50 hover:text-blue-600 rounded-lg transition-colors group">
                    <i data-lucide="bell" class="w-5 h-5 group-hover:text-blue-600"></i>
                    <span class="font-medium">Reminders</span>
                </a>
            </nav>

            <div class="p-4 border-t border-gray-100 space-y-1">
                 <a href="/profile" class="flex items-center gap-3 px-4 py-3 bg-blue-50 text-blue-600 rounded-lg transition-colors">
                    <i data-lucide="user" class="w-5 h-5"></i>
                    <span class="font-medium">Profile</span>
                </a>
                <a href="/settings" class="flex items-center gap-3 px-4 py-3 text-slate-600 hover:bg-slate-50 rounded-lg transition-colors">
                    <i data-lucide="settings" class="w-5 h-5"></i>
                    <span class="font-medium">Settings</span>
                </a>
                <a href="/logout" class="flex items-center gap-3 px-4 py-3 text-red-600 hover:bg-red-50 rounded-lg transition-colors">
                    <i data-lucide="log-out" class="w-5 h-5"></i>
                    <span class="font-medium">Sign Out</span>
                </a>
            </div>
        </aside>

        <main class="flex-1 overflow-y-auto">
            <header class="bg-white border-b border-gray-200 sticky top-0 z-10">
                <div class="max-w-7xl mx-auto px-8 py-5 flex justify-between items-center">
                    <div>
                        <h1 class="text-2xl font-bold text-slate-900">Welcome Back, <%= user.name %></h1>
                        <p class="text-slate-500 text-sm mt-1">Here is what's happening in your vault.</p>
                    </div>
                    
                    <div class="flex items-center gap-4">
                        <a href="/emergency-mode" class="flex items-center gap-2 px-4 py-2 bg-white border border-red-200 text-red-600 rounded-lg hover:bg-red-50 transition font-medium">
                            <i data-lucide="alert-triangle" class="w-4 h-4"></i>
                            Emergency Mode
                        </a>

                        <a href="/upload" class="flex items-center gap-2 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition font-medium shadow-md shadow-blue-200">
                            <i data-lucide="plus" class="w-4 h-4"></i>
                            Upload New
                        </a>
                    </div>
                </div>
            </header>

            <div class="max-w-7xl mx-auto px-8 py-8">
                
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
                    
                    <a href="/documents" class="block bg-white p-6 rounded-xl border border-gray-100 shadow-sm hover:shadow-md hover:border-blue-100 transition group">
                        <div class="flex justify-between items-start mb-4">
                            <div class="text-slate-500 font-medium text-sm">Total Documents</div>
                            <div class="p-2 bg-blue-50 text-blue-600 rounded-lg group-hover:bg-blue-600 group-hover:text-white transition">
                                <i data-lucide="file-text" class="w-4 h-4"></i>
                            </div>
                        </div>
                        <div class="text-3xl font-bold text-slate-900"><%= totalDocs || 0 %></div>
                        <div class="text-xs text-slate-400 mt-2">View all files</div>
                    </a>

                    <a href="/medical-records" class="block bg-white p-6 rounded-xl border border-gray-100 shadow-sm hover:shadow-md hover:border-red-100 transition group">
                        <div class="flex justify-between items-start mb-4">
                            <div class="text-slate-500 font-medium text-sm">Medical Records</div>
                            <div class="p-2 bg-red-50 text-red-500 rounded-lg group-hover:bg-red-500 group-hover:text-white transition">
                                <i data-lucide="heart-pulse" class="w-4 h-4"></i>
                            </div>
                        </div>
                        <div class="text-3xl font-bold text-slate-900"><%= medicalDocs || 0 %></div>
                        <div class="text-xs text-slate-400 mt-2">View medical history</div>
                    </a>

                    <a href="/emergency-contacts" class="block bg-white p-6 rounded-xl border border-gray-100 shadow-sm hover:shadow-md hover:border-amber-100 transition group">
                        <div class="flex justify-between items-start mb-4">
                            <div class="text-slate-500 font-medium text-sm">Emergency Contacts</div>
                            <div class="p-2 bg-amber-50 text-amber-500 rounded-lg group-hover:bg-amber-500 group-hover:text-white transition">
                                <i data-lucide="alert-triangle" class="w-4 h-4"></i>
                            </div>
                        </div>
                        <div class="text-3xl font-bold text-slate-900"><%= contactsCount || 0 %></div>
                        <div class="text-xs text-slate-400 mt-2">Trusted contacts</div>
                    </a>

                    <a href="/reminders" class="block bg-white p-6 rounded-xl border border-gray-100 shadow-sm hover:shadow-md hover:border-indigo-100 transition group">
                        <div class="flex justify-between items-start mb-4">
                            <div class="text-slate-500 font-medium text-sm">Pending Reminders</div>
                            <div class="p-2 bg-indigo-50 text-indigo-500 rounded-lg group-hover:bg-indigo-500 group-hover:text-white transition">
                                <i data-lucide="clock" class="w-4 h-4"></i>
                            </div>
                        </div>
                        <div class="text-3xl font-bold text-slate-900"><%= pendingReminders ? pendingReminders.length : 0 %></div>
                        <div class="text-xs text-slate-400 mt-2">Due in next 10 days</div>
                    </a>
                </div>

                <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                    
                    <div class="lg:col-span-2">
                        <div class="bg-white rounded-xl border border-gray-100 shadow-sm">
                            <div class="p-6 border-b border-gray-100 flex justify-between items-center">
                                <div>
                                    <h2 class="text-lg font-bold text-slate-900">Recent Documents</h2>
                                    <p class="text-sm text-slate-500">Recently accessed or uploaded files</p>
                                </div>
                                <a href="/documents" class="text-sm font-semibold text-blue-600 hover:text-blue-700">View All</a>
                            </div>

                            <div class="divide-y divide-gray-50">
                                <% if(!recentDocs || recentDocs.length === 0) { %>
                                    <div class="p-8 text-center text-slate-400 text-sm">
                                        No documents found. <a href="/upload" class="text-blue-600 font-semibold">Upload one now?</a>
                                    </div>
                                <% } else { %>
                                    <% recentDocs.forEach(doc => { %>
                                        <div class="p-4 hover:bg-slate-50 transition flex items-center gap-4 group">
                                            <div class="p-3 bg-blue-50 text-blue-600 rounded-lg">
                                                <i data-lucide="<%= doc.category === 'medical' ? 'heart-pulse' : 'file-text' %>" class="w-5 h-5"></i>
                                            </div>
                                            <div class="flex-1">
                                                <h3 class="font-medium text-slate-900 truncate"><%= doc.title %></h3>
                                                <p class="text-xs text-slate-500 capitalize"><%= doc.category %> • <%= new Date(doc.createdAt).toLocaleDateString() %></p>
                                            </div>
                                            
                                            <div class="flex items-center gap-2 opacity-0 group-hover:opacity-100 transition-opacity">
                                                <a href="/view-document/<%= doc._id %>" target="_blank" title="Open" class="p-2 text-slate-400 hover:text-blue-600">
                                                    <i data-lucide="external-link" class="w-4 h-4"></i>
                                                </a>
                                                <a href="/delete-doc/<%= doc._id %>" title="Delete" onclick="return confirm('Delete this file?')" class="p-2 text-slate-400 hover:text-red-600">
                                                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                                                </a>
                                            </div>
                                        </div>
                                    <% }) %>
                                <% } %>
                            </div>
                        </div>
                    </div>

                    <div class="space-y-6">
                        
                        <div class="bg-orange-50 border border-orange-100 rounded-xl p-6">
                            <div class="flex items-center gap-2 text-orange-700 font-bold mb-2">
                                <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                                Emergency Access
                            </div>
                            <p class="text-orange-800/80 text-sm mb-4 leading-relaxed">
                                Quick access to trusted contacts and critical info.
                            </p>
                            <a href="/emergency-mode" class="block w-full text-center py-2.5 bg-orange-500 hover:bg-orange-600 text-white font-semibold rounded-lg shadow-sm transition">
                                Activate Emergency Mode
                            </a>
                            <p class="text-xs text-orange-400 mt-3 text-center">
                                Tap to view trusted contacts immediately.
                            </p>
                        </div>

                        <div class="bg-white rounded-xl border border-gray-100 shadow-sm p-6">
                            <div class="flex justify-between items-center mb-4">
                                <h3 class="font-bold text-slate-900">Upcoming Reminders</h3>
                                <a href="/reminders" class="text-xs text-blue-600 font-semibold hover:underline">View All</a>
                            </div>
                            
                            <div class="space-y-4">
                                <% if(!pendingReminders || pendingReminders.length === 0) { %>
                                    <div class="text-xs text-slate-400 text-center py-4">No upcoming reminders.</div>
                                <% } else { %>
                                    <% pendingReminders.forEach(reminder => { 
                                        const daysLeft = Math.ceil((new Date(reminder.reminderDate) - new Date()) / (1000 * 60 * 60 * 24));
                                    %>
                                        <div class="flex gap-3 items-start">
                                            <div class="mt-1 w-2 h-2 rounded-full <%= daysLeft <= 3 ? 'bg-red-500' : 'bg-blue-500' %>"></div>
                                            <div>
                                                <p class="text-sm font-medium text-slate-900"><%= reminder.reminderNote %></p>
                                                <p class="text-xs text-slate-500">
                                                    <%= daysLeft < 0 ? 'Overdue' : (daysLeft === 0 ? 'Today' : 'In ' + daysLeft + ' days') %>
                                                </p>
                                            </div>
                                        </div>
                                    <% }) %>
                                <% } %>
                            </div>
                        </div>

                    </div>
                </div>

            </div>
        </main>
    </div>

    <script>
        lucide.createIcons();
    </script>
</body>
</html>
