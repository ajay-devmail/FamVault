<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FamVault - Set New Password</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
        }
    </script>
</head>

<body
    class="bg-gray-50 dark:bg-[#092e3d] flex items-center justify-center min-h-screen font-sans text-slate-800 dark:text-white transition-colors duration-300">

    <div class="absolute top-6 right-6 z-20">
        <button onclick="toggleTheme()"
            class="p-2 rounded-full bg-white/80 dark:bg-black/20 text-slate-800 dark:text-white hover:bg-white dark:hover:bg-black/40 transition-all shadow-sm backdrop-blur-sm">
            <i data-lucide="sun" class="w-5 h-5 hidden dark:block"></i>
            <i data-lucide="moon" class="w-5 h-5 dark:hidden"></i>
        </button>
    </div>

    <div
        class="w-full max-w-md bg-white dark:bg-[#191919] p-10 rounded-2xl shadow-xl dark:shadow-2xl text-center border border-gray-100 dark:border-white/5 transition-colors">
        <div class="flex items-center justify-center gap-1 mb-2 text-3xl font-bold">
            <span class="text-blue-600 dark:text-blue-500">Fam</span><span
                class="text-slate-900 dark:text-white">Vault</span>
        </div>
        <div class="text-slate-500 dark:text-gray-400 text-sm mb-10">Secure your family's digital legacy</div>

        <h2 class="text-left text-xl font-semibold mb-6 text-slate-800 dark:text-white">Set New Password</h2>

        <form action="/update-password" method="POST" class="text-left">
            <input type="hidden" name="email" value="<%= email %>">

            <div class="mb-6">
                <label
                    class="block text-xs font-bold text-slate-500 dark:text-gray-400 uppercase tracking-widest mb-2">New
                    Password</label>
                <input type="password" name="newPassword" placeholder="••••••••" required minlength="8"
                    class="w-full p-3.5 bg-gray-50 dark:bg-[#292929] border border-gray-200 dark:border-white/10 rounded-xl text-slate-900 dark:text-white text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 dark:focus:border-blue-500 transition-all placeholder:text-slate-400 dark:placeholder:text-gray-500 appearance-none">
                <span class="text-slate-500 dark:text-gray-500 text-xs block mt-1.5">Must be at least 8 characters
                    long.</span>
            </div>

            <div class="mb-6">
                <label
                    class="block text-xs font-bold text-slate-500 dark:text-gray-400 uppercase tracking-widest mb-2">Confirm
                    Password</label>
                <input type="password" name="confirmPassword" placeholder="••••••••" required
                    class="w-full p-3.5 bg-gray-50 dark:bg-[#292929] border border-gray-200 dark:border-white/10 rounded-xl text-slate-900 dark:text-white text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 dark:focus:border-blue-500 transition-all placeholder:text-slate-400 dark:placeholder:text-gray-500 appearance-none">
            </div>

            <button type="submit"
                class="w-full py-3.5 bg-blue-600 hover:bg-blue-700 text-white border-none rounded-xl text-base font-semibold cursor-pointer mt-2 shadow-lg shadow-blue-500/20 transition-colors">
                Update Password
            </button>
        </form>

        <div class="mt-8 text-sm text-slate-500 dark:text-gray-400">
            Remembered your password? <a href="/login"
                class="text-blue-600 dark:text-blue-500 no-underline font-medium hover:underline">Sign In</a>
        </div>
    </div>

    <script>
        lucide.createIcons();

        // Theme Logic
        function toggleTheme() {
            if (document.documentElement.classList.contains('dark')) {
                document.documentElement.classList.remove('dark');
                localStorage.theme = 'light';
            } else {
                document.documentElement.classList.add('dark');
                localStorage.theme = 'dark';
            }
        }

        // Initialize Theme
        if (localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
            document.documentElement.classList.add('dark');
        } else {
            document.documentElement.classList.remove('dark');
        }
    </script>
</body>

</html>
