<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile - FamVault</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');

        body {
            font-family: 'Inter', sans-serif;
        }
    </style>
    <script>
        tailwind.config = {
            darkMode: 'class',
        }
    </script>
</head>

<body class="bg-gray-50 dark:bg-slate-900 text-slate-800 dark:text-white font-inter transition-colors duration-200">

    <div class="flex h-screen overflow-hidden">

        <aside
            class="w-64 bg-white dark:bg-slate-900 border-r border-gray-200 dark:border-white/10 hidden md:flex flex-col transition-colors duration-200">
            <div class="p-6 flex items-center gap-3">
                <div class="bg-blue-600 p-1.5 rounded-lg">
                    <i data-lucide="shield" class="w-6 h-6 text-white fill-current"></i>
                </div>
                <span class="text-xl font-bold text-slate-900 dark:text-white transition-colors">FamVault</span>
            </div>

            <nav class="flex-1 px-4 space-y-1 mt-4">
                <a href="/overview"
                    class="flex items-center gap-3 px-4 py-3 text-slate-600 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-white/5 hover:text-blue-600 dark:hover:text-blue-400 rounded-lg transition-colors group">
                    <i data-lucide="layout-dashboard"
                        class="w-5 h-5 group-hover:text-blue-600 dark:group-hover:text-blue-400"></i>
                    <span class="font-medium">Overview</span>
                </a>
                <a href="/documents"
                    class="flex items-center gap-3 px-4 py-3 text-slate-600 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-white/5 hover:text-blue-600 dark:hover:text-blue-400 rounded-lg transition-colors group">
                    <i data-lucide="file-text"
                        class="w-5 h-5 group-hover:text-blue-600 dark:group-hover:text-blue-400"></i>
                    <span class="font-medium">Documents</span>
                </a>
                <a href="/medical-records"
                    class="flex items-center gap-3 px-4 py-3 text-slate-600 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-white/5 hover:text-blue-600 dark:hover:text-blue-400 rounded-lg transition-colors group">
                    <i data-lucide="heart-pulse"
                        class="w-5 h-5 group-hover:text-blue-600 dark:group-hover:text-blue-400"></i>
                    <span class="font-medium">Medical Records</span>
                </a>
                <a href="/emergency-contacts"
                    class="flex items-center gap-3 px-4 py-3 text-slate-600 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-white/5 hover:text-blue-600 dark:hover:text-blue-400 rounded-lg transition-colors group">
                    <i data-lucide="phone" class="w-5 h-5 group-hover:text-blue-600 dark:group-hover:text-blue-400"></i>
                    <span class="font-medium">Emergency Contacts</span>
                </a>
                <a href="/reminders"
                    class="flex items-center gap-3 px-4 py-3 text-slate-600 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-white/5 hover:text-blue-600 dark:hover:text-blue-400 rounded-lg transition-colors group">
                    <i data-lucide="bell" class="w-5 h-5 group-hover:text-blue-600 dark:group-hover:text-blue-400"></i>
                    <span class="font-medium">Reminders</span>
                </a>
            </nav>

            <div class="p-4 border-t border-gray-100 dark:border-white/10 space-y-1">
                <a href="/profile"
                    class="flex items-center gap-3 px-4 py-3 bg-blue-600 text-white rounded-lg transition-colors shadow-sm shadow-blue-200 dark:shadow-blue-900/20">
                    <% if (user.profilePic) { %>
                        <img src="<%= user.profilePic %>" alt="Profile"
                            class="w-6 h-6 rounded-full object-cover ring-2 ring-white/10">
                        <% } else { %>
                            <i data-lucide="user" class="w-5 h-5"></i>
                            <% } %>
                                <span class="font-medium">Profile</span>
                </a>
                <a href="/settings"
                    class="flex items-center gap-3 px-4 py-3 text-slate-600 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-white/5 hover:text-blue-600 dark:hover:text-blue-400 rounded-lg transition-colors group">
                    <i data-lucide="settings"
                        class="w-5 h-5 group-hover:text-blue-600 dark:group-hover:text-blue-400"></i>
                    <span class="font-medium">Settings</span>
                </a>
                <a href="/logout"
                    class="flex items-center gap-3 px-4 py-3 text-red-600 dark:text-red-400 hover:bg-red-50 dark:hover:bg-red-500/10 rounded-lg transition-colors">
                    <i data-lucide="log-out" class="w-5 h-5"></i>
                    <span class="font-medium">Sign Out</span>
                </a>
            </div>
        </aside>

        <main class="flex-1 overflow-y-auto bg-gray-50 dark:bg-slate-900/50 transition-colors">
            <header
                class="h-20 border-b border-gray-200 dark:border-white/10 flex items-center justify-between px-8 bg-white dark:bg-slate-900 sticky top-0 z-20 transition-colors">
                <div>
                    <h1 class="text-2xl font-bold text-slate-900 dark:text-white">Edit Profile</h1>
                    <p class="text-slate-500 dark:text-slate-400 text-sm">Update your personal information</p>
                </div>
                <!-- Theme Toggle & Actions -->
                <div class="flex items-center gap-4">
                    <button onclick="toggleTheme()"
                        class="p-2 rounded-lg bg-gray-100 dark:bg-slate-800 text-slate-600 dark:text-slate-400 hover:bg-gray-200 dark:hover:text-white transition-colors">
                        <i data-lucide="sun" class="w-5 h-5 hidden dark:block"></i>
                        <i data-lucide="moon" class="w-5 h-5 dark:hidden"></i>
                    </button>
                    <a href="/profile"
                        class="flex items-center gap-2 px-4 py-2 border border-gray-300 dark:border-white/10 text-slate-600 dark:text-slate-300 rounded-lg font-medium hover:bg-gray-50 dark:hover:bg-white/5 hover:text-slate-900 dark:hover:text-white transition-colors">
                        Cancel
                    </a>
                </div>
            </header>

            <div class="p-8">
                <div class="max-w-4xl mx-auto">
                    <form action="/edit-profile" method="POST" enctype="multipart/form-data"
                        class="bg-white dark:bg-slate-800 border border-gray-200 dark:border-white/10 rounded-3xl overflow-hidden shadow-sm dark:shadow-2xl transition-colors">

                        <div class="h-32 bg-gradient-to-r from-blue-600 via-indigo-600 to-purple-600 relative">
                            <div
                                class="absolute inset-0 bg-[url('https://www.transparenttextures.com/patterns/cubes.png')] opacity-10">
                            </div>
                        </div>

                        <div class="px-8 pb-8">
                            <div class="relative flex justify-between items-end -mt-16 mb-8">
                                <div
                                    class="p-2 bg-white dark:bg-slate-900 rounded-2xl shadow-xl relative group transition-colors">
                                    <input type="file" name="profilePic" id="profilePicInput" class="hidden"
                                        accept="image/*" onchange="previewImage(this)">
                                    <div onclick="document.getElementById('profilePicInput').click()"
                                        class="w-32 h-32 bg-gray-100 dark:bg-slate-800 rounded-xl flex items-center justify-center overflow-hidden cursor-pointer relative border-2 border-transparent group-hover:border-blue-500 transition-all">

                                        <% if (user.profilePic) { %>
                                            <img id="profilePreview" src="<%= user.profilePic %>" alt="Profile"
                                                class="w-full h-full object-cover">
                                            <% } else { %>
                                                <div id="defaultIcon">
                                                    <i data-lucide="user"
                                                        class="w-12 h-12 text-slate-400 dark:text-slate-600 group-hover:text-blue-400 transition-colors"></i>
                                                </div>
                                                <img id="profilePreview" src="" alt="Profile Preview"
                                                    class="w-full h-full object-cover hidden">
                                                <% } %>

                                                    <div
                                                        class="absolute inset-0 bg-black/60 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity">
                                                        <i data-lucide="camera" class="w-8 h-8 text-white"></i>
                                                    </div>
                                    </div>
                                </div>
                                <button type="submit"
                                    class="flex items-center gap-2 px-6 py-2.5 bg-blue-600 hover:bg-blue-700 text-white rounded-xl font-bold shadow-lg shadow-blue-500/30 dark:shadow-blue-900/30 transition-all">
                                    <i data-lucide="save" class="w-4 h-4"></i>
                                    Save Changes
                                </button>
                            </div>

                            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                                <div class="space-y-1">
                                    <label class="text-xs font-bold text-slate-500 uppercase tracking-wider">Full
                                        Name</label>
                                    <input type="text" name="name" value="<%= user.name %>"
                                        class="w-full text-lg font-medium text-slate-900 dark:text-white bg-gray-50 dark:bg-slate-800/50 border border-gray-200 dark:border-white/10 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all placeholder:text-slate-400 dark:placeholder:text-slate-600">
                                </div>

                                <div class="space-y-1">
                                    <label class="text-xs font-bold text-slate-500 uppercase tracking-wider">Email
                                        Address</label>
                                    <div
                                        class="text-lg font-medium text-slate-500 dark:text-slate-400 bg-gray-100 dark:bg-slate-800/30 border border-gray-200 dark:border-white/5 rounded-xl px-4 py-3 cursor-not-allowed select-none">
                                        <%= user.email %>
                                    </div>
                                </div>

                                <div class="space-y-1">
                                    <label class="text-xs font-bold text-slate-500 uppercase tracking-wider">Phone
                                        Number</label>
                                    <input type="text" name="phone" value="<%= user.phone %>"
                                        placeholder="+91 XXXXX-XXXXX"
                                        class="w-full text-lg font-medium text-slate-900 dark:text-white bg-gray-50 dark:bg-slate-800/50 border border-gray-200 dark:border-white/10 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all placeholder:text-slate-400 dark:placeholder:text-slate-600">
                                </div>

                                <div class="space-y-1">
                                    <label class="text-xs font-bold text-slate-500 uppercase tracking-wider">Date of
                                        Birth</label>
                                    <input type="date" name="dob" value="<%= user.dob %>"
                                        class="w-full text-lg font-medium text-slate-900 dark:text-white bg-gray-50 dark:bg-slate-800/50 border border-gray-200 dark:border-white/10 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all [color-scheme:light] dark:[color-scheme:dark]">
                                </div>

                                <div class="space-y-1">
                                    <label
                                        class="text-xs font-bold text-slate-500 uppercase tracking-wider">Gender</label>
                                    <div class="relative">
                                        <select name="gender"
                                            class="w-full text-lg font-medium text-slate-900 dark:text-white bg-gray-50 dark:bg-slate-800/50 border border-gray-200 dark:border-white/10 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all appearance-none cursor-pointer">
                                            <option value=""
                                                class="bg-white dark:bg-slate-900 text-slate-500 dark:text-slate-400"
                                                <%=!user.gender ? 'selected' : '' %>>Select Gender</option>
                                            <option value="Male" class="bg-white dark:bg-slate-900"
                                                <%=user.gender==='Male' ? 'selected' : '' %>>Male</option>
                                            <option value="Female" class="bg-white dark:bg-slate-900"
                                                <%=user.gender==='Female' ? 'selected' : '' %>>Female</option>
                                            <option value="Other" class="bg-white dark:bg-slate-900"
                                                <%=user.gender==='Other' ? 'selected' : '' %>>Other</option>
                                        </select>
                                        <i data-lucide="chevron-down"
                                            class="absolute right-4 top-1/2 -translate-y-1/2 w-5 h-5 text-slate-500 pointer-events-none"></i>
                                    </div>
                                </div>

                                <div class="space-y-1">
                                    <label class="text-xs font-bold text-slate-500 uppercase tracking-wider">Family
                                        Role</label>
                                    <div class="relative">
                                        <select name="role"
                                            class="w-full text-lg font-medium text-slate-900 dark:text-white bg-gray-50 dark:bg-slate-800/50 border border-gray-200 dark:border-white/10 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all appearance-none cursor-pointer">
                                            <option value=""
                                                class="bg-white dark:bg-slate-900 text-slate-500 dark:text-slate-400"
                                                <%=!user.role ? 'selected' : '' %>>Select Role</option>
                                            <option value="Father" class="bg-white dark:bg-slate-900"
                                                <%=user.role==='Father' ? 'selected' : '' %>>Father</option>
                                            <option value="Mother" class="bg-white dark:bg-slate-900"
                                                <%=user.role==='Mother' ? 'selected' : '' %>>Mother</option>
                                            <option value="Son" class="bg-white dark:bg-slate-900" <%=user.role==='Son'
                                                ? 'selected' : '' %>>Son</option>
                                            <option value="Daughter" class="bg-white dark:bg-slate-900"
                                                <%=user.role==='Daughter' ? 'selected' : '' %>>Daughter</option>
                                            <option value="Guardian" class="bg-white dark:bg-slate-900"
                                                <%=user.role==='Guardian' ? 'selected' : '' %>>Guardian</option>
                                        </select>
                                        <i data-lucide="chevron-down"
                                            class="absolute right-4 top-1/2 -translate-y-1/2 w-5 h-5 text-slate-500 pointer-events-none"></i>
                                    </div>
                                </div>

                                <div class="space-y-1">
                                    <label class="text-xs font-bold text-slate-500 uppercase tracking-wider">Blood
                                        Group</label>
                                    <div class="relative">
                                        <select name="bloodGroup"
                                            class="w-full text-lg font-bold text-red-600 dark:text-red-500 bg-red-50 dark:bg-red-500/10 border border-red-200 dark:border-red-500/20 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition-all appearance-none cursor-pointer">
                                            <option value=""
                                                class="bg-white dark:bg-slate-900 text-slate-500 dark:text-slate-400"
                                                <%=!user.bloodGroup ? 'selected' : '' %>>Select</option>
                                            <option value="A+" class="bg-white dark:bg-slate-900"
                                                <%=user.bloodGroup==='A+' ? 'selected' : '' %>>A+</option>
                                            <option value="A-" class="bg-white dark:bg-slate-900"
                                                <%=user.bloodGroup==='A-' ? 'selected' : '' %>>A-</option>
                                            <option value="B+" class="bg-white dark:bg-slate-900"
                                                <%=user.bloodGroup==='B+' ? 'selected' : '' %>>B+</option>
                                            <option value="B-" class="bg-white dark:bg-slate-900"
                                                <%=user.bloodGroup==='B-' ? 'selected' : '' %>>B-</option>
                                            <option value="AB+" class="bg-white dark:bg-slate-900"
                                                <%=user.bloodGroup==='AB+' ? 'selected' : '' %>>AB+</option>
                                            <option value="AB-" class="bg-white dark:bg-slate-900"
                                                <%=user.bloodGroup==='AB-' ? 'selected' : '' %>>AB-</option>
                                            <option value="O+" class="bg-white dark:bg-slate-900"
                                                <%=user.bloodGroup==='O+' ? 'selected' : '' %>>O+</option>
                                            <option value="O-" class="bg-white dark:bg-slate-900"
                                                <%=user.bloodGroup==='O-' ? 'selected' : '' %>>O-</option>
                                        </select>
                                        <i data-lucide="chevron-down"
                                            class="absolute right-4 top-1/2 -translate-y-1/2 w-5 h-5 text-red-500 pointer-events-none"></i>
                                    </div>
                                </div>

                                <div class="space-y-1 md:col-span-2">
                                    <label class="text-xs font-bold text-slate-500 uppercase tracking-wider">Residential
                                        Address</label>
                                    <textarea name="address" rows="3" placeholder="Enter your full address here"
                                        class="w-full text-lg font-medium text-slate-900 dark:text-white bg-gray-50 dark:bg-slate-800/50 border border-gray-200 dark:border-white/10 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all placeholder:text-slate-400 dark:placeholder:text-slate-600 resize-none"><%= user.address %></textarea>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </main>
    </div>
    <script>
        lucide.createIcons();

        // Theme Logic
        function toggleTheme() {
            if (document.documentElement.classList.contains('dark')) {
                document.documentElement.classList.remove('dark');
                localStorage.theme = 'light';
            } else {
                document.documentElement.classList.add('dark');
                localStorage.theme = 'dark';
            }
        }

        // Initialize Theme
        if (localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
            document.documentElement.classList.add('dark');
        } else {
            document.documentElement.classList.remove('dark');
        }

        function previewImage(input) {
            if (input.files && input.files[0]) {
                const reader = new FileReader();

                reader.onload = function (e) {
                    const preview = document.getElementById('profilePreview');
                    const defaultIcon = document.getElementById('defaultIcon');

                    preview.src = e.target.result;
                    preview.classList.remove('hidden');
                    if (defaultIcon) defaultIcon.classList.add('hidden');
                }

                reader.readAsDataURL(input.files[0]);
            }
        }
    </script>
</body>

</html>
