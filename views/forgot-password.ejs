<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FamVault - Forgot Password</title>
    <style>
        :root {
            /* Exact color matching from your image */
            --bg-dark-blue: #0a2533; 
            --card-gray: #1a1a1a;
            --input-gray: #2a2a2a;
            --logo-blue: #4a90e2;
            --btn-blue: #2563eb;
            --text-muted: #888888;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background-color: var(--bg-dark-blue);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #ffffff;
        }

        /* Dimension correction to match the vertical card style */
        .card {
            background-color: var(--card-gray);
            width: 440px;
            padding: 48px 40px;
            border-radius: 16px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.4);
            text-align: center;
        }

        .logo {
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 8px;
            display: flex;
            justify-content: center;
            gap: 2px;
        }

        .logo .fam { color: var(--logo-blue); }
        .logo .vault { color: #ffffff; }

        .tagline {
            color: var(--text-muted);
            font-size: 14px;
            margin-bottom: 40px;
        }

        h2 {
            text-align: left;
            font-size: 22px;
            margin-bottom: 30px;
            font-weight: 600;
        }

        .form-group {
            text-align: left;
            margin-bottom: 24px;
        }

        label {
            display: block;
            text-transform: uppercase;
            font-size: 12px;
            color: var(--text-muted);
            margin-bottom: 10px;
            font-weight: 600;
            letter-spacing: 0.5px;
        }

        input {
            width: 100%;
            padding: 14px 16px;
            background-color: var(--input-gray);
            border: 1px solid #333;
            border-radius: 10px;
            color: white;
            font-size: 15px;
            box-sizing: border-box;
            outline: none;
        }

        .btn-submit {
            width: 100%;
            padding: 14px;
            background-color: var(--btn-blue);
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            margin-top: 10px;
            box-shadow: 0 4px 12px rgba(37, 99, 235, 0.2);
        }

        .btn-submit:hover {
            background-color: #1d4ed8;
        }

        .footer {
            margin-top: 30px;
            font-size: 14px;
            color: var(--text-muted);
        }

        .footer a {
            color: var(--btn-blue);
            text-decoration: none;
            font-weight: 500;
        }
    </style>
</head>
<body>

    <div class="card">
        <div class="logo">
            <span class="fam">Fam</span><span class="vault">Vault</span>
        </div>
        <div class="tagline">Secure your family's digital legacy</div>

        <h2>Forgot Password</h2>

        <form action="/forgot-password" method="POST">
            <div class="form-group">
                <label>Email Address</label>
                <input type="email" name="email" placeholder="vaibhavsingh789g@gmail.com" required>
            </div>

            <button type="submit" class="btn-submit">Send OTP</button>
        </form>

        <div class="footer">
            Already have an account? <a href="/login">Sign In</a>
        </div>
    </div>

</body>
</html>
