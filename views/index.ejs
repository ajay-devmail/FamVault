<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-cyan-950 flex items-center justify-center w-full min-h-screen font-sans text-white p-4">

    <div class="w-full max-w-md bg-zinc-900 border border-zinc-800 rounded-2xl shadow-2xl p-8">

        <div class="text-center mb-10">
            <h1 class="text-3xl font-bold tracking-tighter text-blue-500">Fam<span class="text-white">Vault</span></h1>
            <p class="text-zinc-500 text-sm mt-2">Secure your family's digital legacy</p>
        </div>

        <h2 class="text-xl font-semibold mb-6">Create your account</h2>

        <form action="/register" method="post" class="flex flex-col space-y-5">

            <div class="flex flex-col">
                <label class="text-xs font-semibold text-zinc-400 uppercase tracking-wider mb-1 ml-1">Full Name</label>
                <input
                    class="w-full border border-zinc-700 rounded-xl px-4 py-3 bg-zinc-800 text-white placeholder-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-600 focus:border-transparent transition-all"
                    type="text" name="name" placeholder="Username">
            </div>

            <div class="flex flex-col">
                <label class="text-xs font-semibold text-zinc-400 uppercase tracking-wider mb-1 ml-1">Email
                    Address</label>
                <input
                    class="w-full border border-zinc-700 rounded-xl px-4 py-3 bg-zinc-800 text-white placeholder-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-600 focus:border-transparent transition-all"
                    type="email" name="email" placeholder="Email">
            </div>

            <div class="flex flex-col">
                <label class="text-xs font-semibold text-zinc-400 uppercase tracking-wider mb-1 ml-1">Create
                    Password</label>
                <input
                    class="w-full border border-zinc-700 rounded-xl px-4 py-3 bg-zinc-800 text-white placeholder-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-600 focus:border-transparent transition-all"
                    type="password" name="password" placeholder="••••••••">
                <p class="text-[10px] text-zinc-500 mt-1 ml-1">Must be at least 8 characters long.</p>
            </div>

            <button
                class="w-full bg-blue-600 hover:bg-blue-500 text-white font-bold py-3 px-4 rounded-xl shadow-lg shadow-blue-900/20 transform transition active:scale-[0.97] mt-4"
                type="submit">
                Create Account
            </button>
        </form>

        <div class="mt-8 pt-6 border-t border-zinc-800 text-center">
            <p class="text-zinc-500 text-sm">
                Already have an account? <a href="/login"
                    class="text-blue-500 hover:text-blue-400 font-medium underline-offset-4 hover:underline">Sign In</a>
            </p>
        </div>
    </div>

</body>

</html>
