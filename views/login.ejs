<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-950 flex items-center justify-center w-full h-screen font-sans text-white">

    <div class="w-full max-w-md p-8 bg-zinc-900 border border-zinc-800 rounded-2xl shadow-2xl">
        <div class="text-center mb-8">
            <h1 class="text-3xl font-extrabold tracking-tight text-blue-500 ">Fam<span class="text-white">Vault</span>
            </h1>
            <p class="text-zinc-400 mt-2">Secure access to your family vault</p>
        </div>

        <h3 class="text-lg font-semibold mb-6 text-center">Login to Your Account</h3>

        <form action="/login" method="post" class="flex flex-col space-y-4">
            <div>
                <label class="block text-sm font-medium text-zinc-400 mb-1">Email Address</label>
                <input
                    class="w-full border border-zinc-700 rounded-lg px-4 py-3 bg-zinc-800 text-white placeholder-zinc-500 focus:outline-none focus:ring-2 focus:ring-blue-600 transition-all"
                    type="email" name="email" placeholder="name@example.com">
            </div>

            <div>
                <label class="block text-sm font-medium text-zinc-400 mb-1">Password</label>
                <input
                    class="w-full border border-zinc-700 rounded-lg px-4 py-3 bg-zinc-800 text-white placeholder-zinc-500 focus:outline-none focus:ring-2 focus:ring-blue-600 transition-all"
                    type="password" name="password" placeholder="••••••••">
            </div>

            <button
                class="w-full mt-4 bg-blue-600 hover:bg-blue-500 text-white font-bold py-3 rounded-lg shadow-lg transform transition active:scale-[0.98]"
                type="submit">
                Sign In
            </button>
        </form>

        <p class="text-center text-sm text-zinc-500 mt-8">
            Don't have an account? <a href="/register" class="text-blue-500 hover:underline">Create one</a>
        </p>
    </div>

</body>

</html>