<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - FamVault</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
</head>
<body class="bg-gray-50 font-sans text-slate-800">

    <div class="flex h-screen">
        <aside class="w-64 bg-white border-r p-6 hidden md:block">
            <h1 class="text-xl font-bold mb-8 flex items-center gap-2">
                <i data-lucide="shield" class="text-blue-600"></i> FamVault
            </h1>
            <a href="/overview" class="block mb-4 text-slate-500 hover:text-blue-600">← Back to Dashboard</a>
        </aside>

        <main class="flex-1 overflow-y-auto p-8">
            <div class="max-w-6xl mx-auto">
                
                <div class="flex justify-between items-center mb-8">
                    <h1 class="text-3xl font-bold text-slate-900"><%= title %></h1>
                    <a href="/upload" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 flex items-center gap-2">
                        <i data-lucide="plus" class="w-4 h-4"></i> Upload
                    </a>
                </div>

                <form action="/documents" method="GET" class="mb-8">
                    <div class="relative">
                        <i data-lucide="search" class="absolute left-3 top-3 w-5 h-5 text-slate-400"></i>
                        <input type="text" name="search" value="<%= search %>" placeholder="Search by document name..." 
                        class="w-full pl-10 pr-4 py-3 rounded-xl border border-gray-200 focus:outline-none focus:ring-2 focus:ring-blue-100 shadow-sm">
                    </div>
                </form>

                <% if(docs.length === 0) { %>
                    <div class="text-center py-20 text-slate-400">
                        <i data-lucide="folder-open" class="w-12 h-12 mx-auto mb-3 opacity-50"></i>
                        <p>No documents found.</p>
                    </div>
                <% } else { %>
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                        <% docs.forEach(doc => { %>
                            <div class="bg-white p-5 rounded-xl border border-gray-100 shadow-sm hover:shadow-md transition group">
                                <div class="flex justify-between items-start mb-4">
                                    <div class="p-3 bg-blue-50 text-blue-600 rounded-lg group-hover:bg-blue-600 group-hover:text-white transition">
                                        <i data-lucide="<%= doc.category === 'medical' ? 'heart-pulse' : 'file-text' %>" class="w-6 h-6"></i>
                                    </div>
                                    <div class="flex gap-2">
                                        <a href="/view-document/<%= doc._id %>" target="_blank" class="p-2 text-slate-400 hover:text-blue-600" title="Open">
                                            <i data-lucide="external-link" class="w-4 h-4"></i>
                                        </a>
                                        <a href="/delete-doc/<%= doc._id %>" class="p-2 text-slate-400 hover:text-red-600" title="Delete" onclick="return confirm('Delete this file?')">
                                            <i data-lucide="trash-2" class="w-4 h-4"></i>
                                        </a>
                                    </div>
                                </div>
                                
                                <h3 class="font-bold text-slate-900 truncate"><%= doc.title %></h3>
                                <p class="text-xs text-slate-500 mb-4"><%= new Date(doc.createdAt).toLocaleDateString() %> • <%= doc.fileType.toUpperCase() %></p>

                                <% if(doc.hasReminder) { %>
                                    <div class="text-xs bg-amber-50 text-amber-700 px-3 py-1 rounded-full inline-flex items-center gap-1">
                                        <i data-lucide="clock" class="w-3 h-3"></i> 
                                        <%= doc.reminderNote %> (<%= new Date(doc.reminderDate).toLocaleDateString() %>)
                                    </div>
                                <% } %>
                            </div>
                        <% }) %>
                    </div>
                <% } %>
            </div>
        </main>
    </div>
    <script>lucide.createIcons();</script>
</body>
</html>
