<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - FamVault</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-gray-50 text-slate-800">
<div id="folderModal"
        class="fixed inset-0 bg-slate-900/50 backdrop-blur-sm z-50 hidden flex items-center justify-center">
        <div class="bg-white rounded-2xl p-6 w-full max-w-md shadow-xl mx-4">
            <h3 class="text-xl font-bold text-slate-900 mb-4">Create New Folder</h3>
            <form action="/create-folder" method="POST">
                <input type="text" name="folderName" placeholder="e.g. Taxes, House Docs, Insurance" required
                    class="w-full px-4 py-3 rounded-lg border border-gray-200 focus:outline-none focus:ring-2 focus:ring-blue-500 mb-6">

                <div class="flex gap-3 justify-end">
                    <button type="button" onclick="toggleModal()"
                        class="px-4 py-2 text-slate-600 hover:bg-gray-100 rounded-lg transition">Cancel</button>
                    <button type="submit"
                        class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 shadow-md">
                        Create Folder
                    </button>
                </div>
            </form>
        </div>
    </div>

    <div class="flex h-screen overflow-hidden">
        
        <aside class="w-64 bg-white border-r border-gray-200 hidden md:flex flex-col">
            <div class="p-6 flex items-center gap-3">
                <div class="bg-blue-600 p-1.5 rounded-lg">
                    <i data-lucide="shield" class="w-6 h-6 text-white fill-current"></i>
                </div>
                <span class="text-xl font-bold text-slate-900">FamVault</span>
            </div>

            <nav class="flex-1 px-4 space-y-1 mt-4">
                <a href="/overview" class="flex items-center gap-3 px-4 py-3 text-slate-600 hover:bg-slate-50 hover:text-blue-600 rounded-lg transition-colors group">
                    <i data-lucide="layout-dashboard" class="w-5 h-5 group-hover:text-blue-600"></i>
                    <span class="font-medium">Overview</span>
                </a>

                <% if(title !== 'Medical Records') { %>
                <a href="/documents" class="flex items-center gap-3 px-4 py-3 bg-blue-600 text-white rounded-lg transition-colors shadow-sm shadow-blue-200">
                    <i data-lucide="file-text" class="w-5 h-5"></i>
                    <span class="font-medium">Documents</span>
                </a>
                <% } else { %>
                <a href="/documents" class="flex items-center gap-3 px-4 py-3 text-slate-600 hover:bg-slate-50 hover:text-blue-600 rounded-lg transition-colors group">
                    <i data-lucide="file-text" class="w-5 h-5 group-hover:text-blue-600"></i>
                    <span class="font-medium">Documents</span>
                </a>
                <% } %>

                <% if(title === 'Medical Records') { %>
                <a href="/medical-records" class="flex items-center gap-3 px-4 py-3 bg-blue-600 text-white rounded-lg transition-colors shadow-sm shadow-blue-200">
                    <i data-lucide="heart-pulse" class="w-5 h-5"></i>
                    <span class="font-medium">Medical Records</span>
                </a>
                <% } else { %>
                <a href="/medical-records" class="flex items-center gap-3 px-4 py-3 text-slate-600 hover:bg-slate-50 hover:text-blue-600 rounded-lg transition-colors group">
                    <i data-lucide="heart-pulse" class="w-5 h-5 group-hover:text-blue-600"></i>
                    <span class="font-medium">Medical Records</span>
                </a>
                <% } %>

                <a href="/emergency-contacts" class="flex items-center gap-3 px-4 py-3 text-slate-600 hover:bg-slate-50 hover:text-blue-600 rounded-lg transition-colors group">
                    <i data-lucide="phone" class="w-5 h-5 group-hover:text-blue-600"></i>
                    <span class="font-medium">Emergency Contacts</span>
                </a>

                <a href="/reminders" class="flex items-center gap-3 px-4 py-3 text-slate-600 hover:bg-slate-50 hover:text-blue-600 rounded-lg transition-colors group">
                    <i data-lucide="bell" class="w-5 h-5 group-hover:text-blue-600"></i>
                    <span class="font-medium">Reminders</span>
                </a>
            </nav>

            <div class="p-4 border-t border-gray-100 space-y-1">
                <a href="/settings" class="flex items-center gap-3 px-4 py-3 text-slate-600 hover:bg-slate-50 rounded-lg transition-colors">
                    <i data-lucide="settings" class="w-5 h-5"></i>
                    <span class="font-medium">Settings</span>
                </a>
                <a href="/logout" class="flex items-center gap-3 px-4 py-3 text-red-600 hover:bg-red-50 rounded-lg transition-colors">
                    <i data-lucide="log-out" class="w-5 h-5"></i>
                    <span class="font-medium">Sign Out</span>
                </a>
            </div>
        </aside>

        <main class="flex-1 overflow-y-auto bg-gray-50 p-8">
            <div class="max-w-6xl mx-auto">
                
                <div class="flex justify-between items-center mb-8">
                    <h1 class="text-3xl font-bold text-slate-900"><%= title %></h1>
                    <a href="/upload" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 flex items-center gap-2 shadow-sm shadow-blue-200">
                        <i data-lucide="plus" class="w-4 h-4"></i> Upload
                    </a>
                </div>

                <form action="/documents" method="GET" class="mb-8">
                    <div class="relative">
                        <i data-lucide="search" class="absolute left-3 top-3 w-5 h-5 text-slate-400"></i>
                        <input type="text" name="search" value="<%= search %>" placeholder="Search by document name..." 
                        class="w-full pl-10 pr-4 py-3 rounded-xl border border-gray-200 focus:outline-none focus:ring-2 focus:ring-blue-100 shadow-sm bg-white">
                    </div>
                </form>

                <% if(docs.length === 0) { %>
                    <div class="text-center py-20 text-slate-400">
                        <i data-lucide="folder-open" class="w-12 h-12 mx-auto mb-3 opacity-50"></i>
                        <p>No documents found.</p>
                    </div>
                <% } else { %>
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                        <% docs.forEach(doc => { %>
                            <div class="bg-white p-5 rounded-xl border border-gray-100 shadow-sm hover:shadow-md transition group">
                                <div class="flex justify-between items-start mb-4">
                                    <div class="p-3 bg-blue-50 text-blue-600 rounded-lg group-hover:bg-blue-600 group-hover:text-white transition">
                                        <i data-lucide="<%= doc.category === 'medical' ? 'heart-pulse' : 'file-text' %>" class="w-6 h-6"></i>
                                    </div>
                                    <div class="flex gap-2">
                                        <a href="/view-document/<%= doc._id %>" target="_blank" class="p-2 text-slate-400 hover:text-blue-600" title="Open">
                                            <i data-lucide="external-link" class="w-4 h-4"></i>
                                        </a>
                                        <a href="/delete-doc/<%= doc._id %>" class="p-2 text-slate-400 hover:text-red-600" title="Delete" onclick="return confirm('Delete this file?')">
                                            <i data-lucide="trash-2" class="w-4 h-4"></i>
                                        </a>
                                    </div>
                                </div>
                                
                                <h3 class="font-bold text-slate-900 truncate"><%= doc.title %></h3>
                                <p class="text-xs text-slate-500 mb-4"><%= new Date(doc.createdAt).toLocaleDateString() %> • <%= doc.fileType.toUpperCase() %></p>

                                <% if(doc.hasReminder) { %>
                                    <div class="text-xs bg-amber-50 text-amber-700 px-3 py-1 rounded-full inline-flex items-center gap-1">
                                        <i data-lucide="clock" class="w-3 h-3"></i> 
                                        <%= doc.reminderNote %> (<%= new Date(doc.reminderDate).toLocaleDateString() %>)
                                    </div>
                                <% } %>
                            </div>
                        <% }) %>
                    </div>
                <% } %>
            </div>
        </main>
    </div>
    <script>lucide.createIcons();</script>
</body>
</html>
